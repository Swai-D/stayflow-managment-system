// Default seed entry point — delegates to the full dashboard-ready seeder.
// Running `npm run db:seed` or `npm run db:fresh` now produces realistic data
// across all modules (rooms, bookings, payments, expenses, store, housekeeping, etc.).
import './seed-full'
